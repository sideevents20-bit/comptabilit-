# Dépendances du script de traitement automatique des factures
pdfplumber>=0.11.0        # Extraction du texte des PDF
pandas>=2.0.0             # Génération du tableau de TVA
openpyxl>=3.1.0           # Moteur Excel (.xlsx) utilisé par pandas
python-dotenv>=1.0.0      # Chargement des identifiants depuis le fichier .env
