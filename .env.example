# ============================================================
# Configuration du script de traitement des factures
# Copiez ce fichier vers ".env" puis renseignez vos valeurs.
# Le fichier .env ne doit JAMAIS être partagé ni versionné.
# ============================================================

# --- Identifiants IMAP (obligatoires) -----------------------
# Gmail       : imap.gmail.com  (mot de passe d'application requis)
# Outlook/365 : outlook.office365.com
# OVH         : ssl0.ovh.net
IMAP_HOST=imap.gmail.com
IMAP_PORT=993
IMAP_USER=votre.adresse@gmail.com
IMAP_PASSWORD=votre_mot_de_passe_application

# Boîte à relever (INBOX par défaut).
# Si vous utilisez un libellé Gmail dédié (ex: "Factures" alimenté par un
# filtre automatique), indiquez son nom ici : IMAP_FOLDER=Factures
IMAP_FOLDER=INBOX

# --- Adresse email dédiée aux factures (optionnel) ----------
# Si renseignée, seuls les emails ENVOYÉS À cette adresse sont traités.
# Avec Gmail, l'alias "+factures" fonctionne sans rien créer :
#   side.events2.0+factures@gmail.com arrive dans la même boîte.
# FILTRE_DESTINATAIRE=side.events2.0+factures@gmail.com
FILTRE_DESTINATAIRE=

# --- Chemins (optionnels, valeurs par défaut ci-dessous) ----
# Dossier de classement final des factures par client
DOSSIER_BASE=./Factures_Clients
# Dossier temporaire de téléchargement des PDF
DOSSIER_TEMP=./_temp_factures
# Tableau Excel global de TVA
FICHIER_EXCEL=./tableau_tva_global.xlsx
# Liste des clients (un nom par ligne)
FICHIER_CLIENTS=./clients.txt

# --- Filtrage des emails ------------------------------------
# Mots-clés recherchés dans l'objet (séparés par des virgules)
MOTS_CLES_OBJET=facture,invoice
